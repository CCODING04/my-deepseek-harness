/** Host loader entry for the remote-access settings section browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the settings section. */
export function apply(): void {}
